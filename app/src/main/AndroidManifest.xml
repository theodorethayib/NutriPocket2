<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theo1.nutripocket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity android:name=".MainActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity android:name=".CreateProfileActivity" /> -->

        <activity android:name=".CreateProfileActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <activity android:name=".CreateProfile2Activity" />
        <activity android:name=".activityLevelDescriptionsActivity" />
        <activity android:name=".log" />
        <activity android:name=".BodyTypeDescription" />
        <activity
            android:name=".preferencesActivity"
            android:label="@string/title_activity_preferences" />
        <activity android:name=".Preferences" />
        <activity android:name=".DailyNutritionActivity" />
        <activity android:name=".caloricBreakdown"></activity>
    </application>

</manifest>